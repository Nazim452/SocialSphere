//every slice here in Recoil state mangement library known as atom
import {atom} from 'recoil';
const authScreenAtom = atom({
    key:"authScreenAtom",
    default:"login" , // default at login page screen
})

export default authScreenAtom;

























